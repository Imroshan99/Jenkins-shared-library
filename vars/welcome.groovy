def call(){
echo "welcome from welcome groovy file"

}
def sum(a,b){
echo "sum is ${a} ${b} is equals ${a+b}"
}
def mul(a,b){
echo "mul is ${a} ${b} is equals ${a*b}"
}
def gitClone(link){
 git clone ${link}
}
